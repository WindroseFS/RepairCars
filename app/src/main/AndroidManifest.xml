<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.thorapps.repaircars">

    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@drawable/repaircars_logo_final"
        android:label="@string/app_name"
        android:roundIcon="@drawable/repaircars_logo_final"
        android:supportsRtl="true"
        android:theme="@style/Theme.RepairCars"
        tools:targetApi="31">

        <activity
            android:name=".ChatActivity"
            android:label="Chat"
            android:theme="@style/Theme.RepairCars"
            android:exported="false" />

        <activity android:name=".ProfileActivity" />
        <activity android:name=".LoginOkActivity" />
        <activity android:name=".LoginFailActivity" />
        <activity android:name=".SplashScreenActivity" />

        <activity
            android:name=".ChatActivity"
            android:exported="false" />
        <activity android:name=".ProfileActivity" />
        <activity android:name=".LoginOkActivity" />
        <activity android:name=".LoginFailActivity" />
        <activity android:name=".SplashScreenActivity" />
        <activity
            android:name=".MainActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>