<?xml version="1.0" encoding="utf-8"?>
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav_graph"
    app:startDestination="@id/nav_contacts">

    <!-- Fragmento de Contatos -->
    <fragment
        android:id="@+id/nav_contacts"
        android:name="com.thorapps.repaircars.ui.contacts.ContactsFragment"
        android:label="Contatos"
        tools:layout="@layout/fragment_contacts" />

    <!-- Fragmento de Serviços -->
    <fragment
        android:id="@+id/nav_services"
        android:name="com.thorapps.repaircars.ui.services.ServicesFragment"
        android:label="Serviços"
        tools:layout="@layout/fragment_services" />

    <!-- Fragmento de Clientes -->
    <fragment
        android:id="@+id/nav_customers"
        android:name="com.thorapps.repaircars.ui.customers.CustomersFragment"
        android:label="Clientes"
        tools:layout="@layout/fragment_customers" />

</navigation>